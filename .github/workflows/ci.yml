$workflowContent = @'
name: CI

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run a test
        run: echo "Hello, world!"
'@

$workflowContent | Out-File -FilePath ".github/workflows/ci.yml" -Encoding utf8 -NoNewline